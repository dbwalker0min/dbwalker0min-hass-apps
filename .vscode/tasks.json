{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "HA: Build + Deploy (bump patch, reload)",
            "type": "shell",
            "command": "pwsh",
            "args": [
                "-NoProfile",
                "-ExecutionPolicy",
                "Bypass",
                "-File",
                "${workspaceFolder}\\tools\\ha-build.ps1",
                "${file}"
            ],
            "problemMatcher": [],
            "group": "build"
        }
    ]
}